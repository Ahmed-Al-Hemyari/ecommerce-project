# Express API

This Express.js API serves as the backend for QuickBuy, handling authentication, products, categories, orders, and business logic. It exposes RESTful endpoints and connects to a database to power both the client storefront and admin dashboard.
